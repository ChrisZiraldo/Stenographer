# Cursor API key — get yours at https://cursor.com/dashboard/integrations
# Only required when using the Cursor provider for AI generation.
# Leave unset when using Ollama (local) or the built-in Local (node-llama-cpp) provider.
CURSOR_API_KEY=cursor_...
